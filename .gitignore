*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
# Каталоги сборки модулей. Правило `/build` выше — только корневое, а у
# включённой сборки build-logic каталог свой, и его артефакты (.class, .jar)
# один раз уже уехали в индекс.
build/
/captures
.externalNativeBuild
.cxx
local.properties
secrets.properties

# Кэш сессий Kotlin-плагина — постоянный мусор в git status
.kotlin

# Пароли от release-keystore (см. app/build.gradle)
keystore.properties
*.jks
*.keystore

# Вывод graphify: граф, отчёт и AST-кэш. Пересобирается из исходников,
# в репозитории не нужен (graph.json ~3 МБ, кэш ещё больше).
graphify-out/
