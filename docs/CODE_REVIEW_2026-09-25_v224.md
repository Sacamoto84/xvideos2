# Code Review & Optimization Report — 2026-09-25 (Batch v224)

## Overview & Scope
Batch v224 continues systematic optimization passes 649 to 656 covering `:feature-l`, `:feature-x`, `:feature-r`, and `:app`. The primary focus is eliminating layout allocation hot-paths, hoisting arrangement tokens and static TextAutoSize models, extracting formatted string logic, eliminating CombinedModifier tree nodes, and memoizing recovery subtitles and dropdown interactions.

---

## Detailed Passes & Optimizations

### Pass 649: `:feature-l` — `AlbumFilterDisplay.kt`
- **Hoisted Layout Tokens:** Extracted `FILTER_ROW_ARRANGEMENT`, `ROW_VERTICAL_ALIGNMENT_CENTER`, and `ROW_ARRANGEMENT_SPACE_BETWEEN`.
- **Hoisted Item Title Lambdas:** Hoisted `PRIMARY_ITEM_TITLE` and `SECONDARY_ITEM_TITLE` for reusable item string extractors.
- **Memoized Callbacks:** Memoized `onSelectPrimary` and `onSelectSecondary` callback lambdas avoiding recomposition churn.

### Pass 650: `:feature-l` — `AlbumFilterPresetManager.kt`
- **Extracted Size Label Formatter:** Extracted `PictureCountRank.formatSizeLabel()` private extension to isolate rank-to-string mapping.
- **Pre-sized Collections & Separator Hoisting:** Hoisted `DEFAULT_NAME_SEPARATOR` and `FILTER_SUMMARY_SEPARATOR`, and pre-allocated capacity for summary lists (`ArrayList(6)`, `ArrayList(8)`).

### Pass 651: `:feature-x` — `X_PlayerBottomBar.kt`
- **Avoided CombinedModifier Tree Nodes:** Implemented `if (modifier == Modifier) BOTTOM_BAR_BASE_MODIFIER else modifier.then(BOTTOM_BAR_BASE_MODIFIER)` on row container and `FitModeToggle`.

### Pass 652: `:feature-x` — `ResumePlaybackPill.kt`
- **Surface Modifier Chaining:** Eliminated unconditional `modifier.then(PILL_SURFACE_BASE_MODIFIER)` allocations when default `Modifier` is supplied.

### Pass 653: `:feature-r` — `NicheProfile.kt`
- **Pre-composed Border Modifiers:** Hoisted `ROW_BASE_MODIFIER`, `FOLLOWED_BORDER_MODIFIER`, and `UNFOLLOWED_BORDER_MODIFIER` to eliminate border instantiation per recomposition.
- **Optimized Primitive State Checks:** Replaced redundant `remember(isFollowed)` computations for primitives with direct conditional branches.

### Pass 654: `:feature-r` — `SortByOrder.kt`
- **Deduplicated Order Name Resolution:** Extracted `Order.toDisplayName()` to unify duplicated `when (order)` branching across trigger and dropdown items.
- **Hoisted TextAutoSize & Shapes:** Hoisted `SORT_ORDER_TEXT_AUTO_SIZE`, `SHAPE_8`, `SHAPE_16`, `SHAPE_CIRCLE`, `BORDER_STROKE_1DP`, and color tokens.
- **Memoized Haptics & Dismiss Lambdas:** Memoized `onExpandedChange`, `onDismissRequest`, `onTriggerClick`, and text style.

### Pass 655: `:app` — `CacheSettingsSection.kt`
- **Hoisted Slider Bound Constants:** Hoisted `MIN_RAM_CACHE_PERCENT`, `MAX_RAM_CACHE_PERCENT`, `MIN_DISK_CACHE_SIZE_MB`, and `MAX_DISK_CACHE_SIZE_MB`.
- **Primitive State Check Cleanup:** Replaced redundant `remember(diskCacheEnabled)` with direct primitive ternary.

### Pass 656: `:app` — `RSettingsSection.kt`
- **Memoized Recovery Text:** Wrapped `redDownloadRecoveryText(recoveryReport, isRecoveringDownload)` in `remember` to prevent repeated report formatting in composition.
- **Hoisted Icon Drawables:** Centralized `ICON_RED` and `ICON_HARD_DRIVE` resource constants.

---

## Verification
- `.\gradlew detekt`: **0 issues** (clean).
- `.\gradlew testDebugUnitTest`: **100% tests passing** across `:app`, `:core`, `:feature-l`, `:feature-r`, and `:feature-x`.
