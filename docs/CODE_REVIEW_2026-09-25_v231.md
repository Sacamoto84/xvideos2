# Code Review & Optimization Report — v231 (Passes 705–712)

**Date:** 2026-09-25  
**Scope:** `:feature-l`, `:feature-x`, `:feature-r`, `:app`  
**Quality Gates:** `detekt` (0 issues) & `testDebugUnitTest` (100% pass)

---

## Summary of Passes

### Pass 705 (`:feature-l` — `LCollectionsTopBar.kt`)
- Hoisted `ROW_BASE_MODIFIER` (`Modifier.fillMaxWidth().padding(start = BAR_START_PADDING)`) to avoid per-composition modifier chain allocations.
- Guarded `columnModifier` against empty `Modifier` instances when applying `fillMaxWidth().padding(top = topInset)`.
- Hoisted `selectedMenuItemStyle` and `unselectedMenuItemStyle` `remember` blocks outside the `LCollectionSortOrder.entries.forEach` loop in `DropdownMenu`, reducing `TextStyle.copy` and `remember` overhead from $O(N)$ per sort option to two shared styles.

### Pass 706 (`:feature-l` — `CollectionsGrid.kt`)
- Hoisted `GRID_CELLS = GridCells.Fixed(GRID_COLUMNS)`, `BOX_FILL_MAX_WIDTH`, `ADD_BUTTON_BASE_MODIFIER`, `ADD_ICON_MODIFIER`, `CELL_BASE_MODIFIER`, `PREVIEW_BASE_MODIFIER`, and `TEXT_SPACER_MODIFIER`.
- Guarded `rowModifier` in `CollectionGridCell` (`if (modifier == Modifier) CELL_BASE_MODIFIER else modifier.then(CELL_BASE_MODIFIER)`) to prevent `CombinedModifier` allocations on grid scroll.
- Reused `PREVIEW_BASE_MODIFIER` when `shape == PREVIEW_CORNER_SHAPE` for both `UrlImage` and placeholder `Box`.

### Pass 707 (`:feature-x` — `ScreenXHistory.kt`)
- Hoisted `CARD_BASE_MODIFIER` and `CARD_SELECTED_MODIFIER` (`CARD_BASE_MODIFIER.border(...)`), replacing runtime `.fillMaxWidth().padding(1.dp).aspectRatio(...).background(...).then(cardBorderModifier)` chaining in `HistoryRow` with a direct constant reference (`if (selectionState.isSelected) CARD_SELECTED_MODIFIER else CARD_BASE_MODIFIER`).
- Hoisted `SELECTION_BADGE_SELECTED_MODIFIER`, `SELECTION_BADGE_UNSELECTED_MODIFIER`, and `SELECTION_CHECK_ICON_MODIFIER` in `SelectionCheckBadge`, and `WATCHED_BADGE_BASE_MODIFIER`, `WATCHED_ICON_MODIFIER`, and `WATCHED_TEXT_MODIFIER` in `HistoryWatchedBadge`.
- Memoized `progressProvider` lambda in `HistoryRow` using `remember(progressFraction)` for `LinearProgressIndicator`.

### Pass 708 (`:feature-x` — `ScreenSavedX.kt`)
- Hoisted `FILL_MAX_SIZE_MODIFIER` and `SAVED_ROOT_BASE_MODIFIER` (`Modifier.fillMaxSize().background(Theme.L.grey6)`) with `modifier == Modifier` guard in `X_SavedContent` and `SavedHeader`.
- Replaced `Modifier.weight(1f).then(SAVED_ROW_TITLE_MODIFIER)` with direct `Modifier.weight(1f).padding(horizontal = 8.dp)` in `SavedRow`, eliminating an unnecessary `CombinedModifier` wrapper.
- Memoized `dialogImageUrl` in `X_SavedContent` (`remember(item.id, item.previewImage, saved.downloads)`) to avoid repeated `localPosterPath` lookups while the delete confirmation dialog is visible, and added `item.previewImage` to the `posterUrl` remember key in `LazyColumn` items.

### Pass 709 (`:feature-r` — `SavedCollectionName.kt` & `DialogSubscriptionDelete.kt`)
- In `SavedCollectionName.kt`, replaced `Modifier.padding(top = getTopInsetDp()).then(TOP_BAR_HORIZONTAL_PADDING_MODIFIER)` (which created two `PaddingElement` nodes and a `CombinedModifier`) with a single `TOP_BAR_BASE_MODIFIER.padding(start = TOP_BAR_START_PADDING, top = getTopInsetDp(), end = TOP_BAR_END_PADDING)`.
- In `DialogSubscriptionDelete.kt`, hoisted `URL_IMAGE_MODIFIER = Modifier.fillMaxSize()` and passed it to `UrlImage` inside the avatar container.

### Pass 710 (`:feature-r` — `R_Screen_CollectionTab.kt`)
- Hoisted `TEXT_DELETE_PREFIX`, `TEXT_DELETE_SUFFIX`, `SPAN_STYLE_BOLD`, and `TEXT_FIELD_MODIFIER`.
- Memoized `menuItemStyle = remember { Theme.L.Type.menuItem.copy(color = Color.White) }` once across all three `DropdownMenuItem`s in `R_CollectionDialogsHost`.
- Memoized `coverModifier = remember(size) { Modifier.clip(COVER_ICON_SHAPE).size(size) }` with `modifier == Modifier` guard in `CollectionCoverIcon`.

### Pass 711 (`:app` — `SettingsListItems.kt`)
- Hoisted `SETTINGS_SECTION_TITLE_BASE_MODIFIER` and `SETTINGS_GROUP_BASE_MODIFIER` with `modifier == Modifier` guards in `SettingsSectionTitle` and `SettingsGroup`.
- Memoized `onSliderChange` (`remember(min, max, step)`), `valueRange` (`remember(min, max)`), and `subtitleText` (`remember(currentValue, suffix)`) in `IntSliderSetting` to eliminate lambda and `ClosedFloatingPointRange` allocations during continuous slider scrubbing.

### Pass 712 (`:app` — `AppLockSection.kt`)
- Hoisted `ERROR_TEXT_STYLE`, `DIALOG_BODY_TEXT_STYLE`, `TIMEOUT_SELECTED_TEXT_STYLE`, and `TIMEOUT_UNSELECTED_TEXT_STYLE`, replacing inline `Theme.L.Type.dialogBody.copy(...)` allocations across `CamouflageVerificationDialog`, `AppLockPasswordDialog`, and `AppLockTimeoutItem`.
- Memoized `fieldTextStyle = remember(dialogTheme.bodyColor) { Theme.L.Type.body.copy(color = dialogTheme.bodyColor) }` and guarded `fieldModifier` and `itemBaseModifier` against `CombinedModifier` allocations when `modifier == Modifier`.

---

## Verification

- `./gradlew detekt` — **PASSED** (0 issues)
- `./gradlew testDebugUnitTest` — **PASSED** (100% tests passing across `:core`, `:feature-l`, `:feature-r`, `:feature-x`, `:app`)
