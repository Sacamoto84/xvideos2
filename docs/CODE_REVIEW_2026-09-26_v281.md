# Code Review & Architecture Refactoring Report — Batch 65 (v281)
**Date:** 2026-09-26  
**Scope:** Passes 1105–1112 (`:core`)

---

## 1. Executive Summary

Batch 65 (v281) initiates a review cycle focusing on `:core` non-UI settings, application lock timeout enums, video player configuration models, and background storage gates. It introduces cyclic size navigation, alias getters, and validation helpers (`next`, `prev`, `isLarge`, `isMedium`, `fromOrdinalOrDefault`, `isValidValue`, `isValidDisplayName`) to `ThumbnailsSize`, extracts pure column calculation algorithms and adds navigation routines (`getEnabledColumns`, `calculateNextColumn`, `calculatePrevColumn`, `ColumnSelect_PrevColumn`) to `ColumnSelect`, provides cyclic effect switching, blur shader requirement checks, and localized title lookups (`next`, `prev`, `requiresBlurShader`, `fromOrdinalOrDefault`, `fromTitleOrDefault`, `allTitles`) to `ScrollButtonEffect`, introduces cyclic timeout stepping and alias getters (`next`, `prev`, `isImmediate`, `fromOrdinalOrDefault`, `displayNames`) to `AppLockTimeout`, adds cyclic speed stepping, bounds-clamped speed adjustments, closest speed approximation, screen resize toggling, and submenu queries (`nextSpeed`, `prevSpeed`, `faster`, `slower`, `closestSpeed`, `fromSpeedOrDefault`, `fromDisplayName`, `ScreenResize.toggle`, `PlayerOption.hasSubmenu`) to `PlayerSpeed`, `ScreenResize`, and `PlayerOption`, introduces immutable functional mutators and input sanitization (`withVolume`, `withSpeed`, `withSeek`, `withPlayFromTime`, `toggledPlayPause`, `toggledLoop`, `toggledScreenResize`, `sanitized`, `isAudioEnabled`, `isPlaying`) to `PlayerPlaybackConfig`, and adds status description, cancellation, and job state checks (`cancel`, `hasJob`, `getStatusDescription`) to `StorageCleanupGate`.

---

## 2. Detailed Changes by Pass

### Pass 1105: `ThumbnailsSize` Cyclic Navigation & Validations (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/settings/ThumbnailsSize.kt`
- **Changes:**
  - Added `val isLarge: Boolean get() = this == XMAX`.
  - Added `val isMedium: Boolean get() = this == SMALL`.
  - Added `fun next(): ThumbnailsSize` for cyclic forward navigation.
  - Added `fun prev(): ThumbnailsSize` for cyclic backward navigation.
  - Added `fromOrdinalOrDefault(ordinal: Int, default: ThumbnailsSize = DEFAULT): ThumbnailsSize`.
  - Added `isValidValue(value: String?): Boolean` to check valid persistence values.
  - Added `isValidDisplayName(displayName: String?): Boolean` to validate UI labels.
- **Verification:** Unit tests in `Batch65CoreSettingsAndPlayerTest.kt`.

### Pass 1106: `ColumnSelect` Pure Math & Column Cycling (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/settings/ColumnSelect.kt`
- **Changes:**
  - Extracted `fun getEnabledColumns(flags: List<Boolean>, minCol: Int = 1, maxCol: Int = 4): List<Int>`.
  - Extracted pure function `fun calculateNextColumn(currentIndex: Int, flags: List<Boolean>, minCol: Int = 1, maxCol: Int = 4): Int`.
  - Extracted pure function `fun calculatePrevColumn(currentIndex: Int, flags: List<Boolean>, minCol: Int = 1, maxCol: Int = 4): Int`.
  - Added `fun ColumnSelect_PrevColumn(pref: SettingElementInt, list: SettingElementList<Boolean>)`.
  - Refactored `ColumnSelect_AddColumn` to delegate to `calculateNextColumn`.
- **Verification:** Unit tests in `Batch65CoreSettingsAndPlayerTest.kt`.

### Pass 1107: `ScrollButtonEffect` Shaders & Title Resolvers (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/settings/ScrollButtonEffect.kt`
- **Changes:**
  - Added `val requiresBlurShader: Boolean get() = this != FLAT`.
  - Added `fun next(): ScrollButtonEffect` for cyclic scrolling effect rotation.
  - Added `fun prev(): ScrollButtonEffect` for cyclic backward effect rotation.
  - Added `val allTitles: List<String> = entries.map { it.title }`.
  - Added `fun fromOrdinalOrDefault(ordinal: Int, default: ScrollButtonEffect = DEFAULT): ScrollButtonEffect`.
  - Added `fun fromTitleOrDefault(title: String?, default: ScrollButtonEffect = DEFAULT): ScrollButtonEffect`.
- **Verification:** Unit tests in `Batch65CoreSettingsAndPlayerTest.kt`.

### Pass 1108: `AppLockTimeout` Stepping & Display Names (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/applock/AppLockTimeout.kt`
- **Changes:**
  - Added `val isImmediate: Boolean get() = this == IMMEDIATELY`.
  - Added `fun next(): AppLockTimeout` for cyclic timeout switching.
  - Added `fun prev(): AppLockTimeout` for cyclic backward timeout switching.
  - Added `val displayNames: List<String> = entries.map { it.displayName }`.
  - Added `fun fromOrdinalOrDefault(ordinal: Int, default: AppLockTimeout = DEFAULT): AppLockTimeout`.
- **Verification:** Unit tests in `Batch65CoreSettingsAndPlayerTest.kt`.

### Pass 1109: `PlayerSpeed`, `ScreenResize` & `PlayerOption` Navigation (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/videoplayer/model/enumPlayerSpeed.kt`
- **Changes:**
  - In `PlayerSpeed`: added `nextSpeed()`, `prevSpeed()`, `faster()`, `slower()`, `fromSpeedOrDefault()`, `fromDisplayName()`, `closestSpeed()`, and `fromOrdinalOrDefault()`.
  - In `ScreenResize`: added `toggle()` and `fromOrdinalOrDefault()`.
  - In `PlayerOption`: added `val hasSubmenu: Boolean get() = this != NONE` and `fromOrdinalOrDefault()`.
- **Verification:** Unit tests in `Batch65CoreSettingsAndPlayerTest.kt`.

### Pass 1110: `PlayerPlaybackConfig` Fluent Mutators & Validation (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/videoplayer/model/PlayerPlaybackConfig.kt`
- **Changes:**
  - Added `val isAudioEnabled: Boolean get() = !isMuted`.
  - Added `val isPlaying: Boolean get() = isNotEmpty && !isPause`.
  - Added `fun withVolume(newVolume: Float): PlayerPlaybackConfig` with clamping between `0f..1f`.
  - Added `fun withSpeed(newSpeed: PlayerSpeed): PlayerPlaybackConfig`.
  - Added `fun withSeek(time: Float?): PlayerPlaybackConfig` with minimum bound at `0f`.
  - Added `fun withPlayFromTime(time: Float?): PlayerPlaybackConfig` with minimum bound at `0f`.
  - Added `fun toggledPlayPause(): PlayerPlaybackConfig`.
  - Added `fun toggledLoop(): PlayerPlaybackConfig`.
  - Added `fun toggledScreenResize(): PlayerPlaybackConfig`.
  - Added `fun sanitized(): PlayerPlaybackConfig` for defensive boundary validation.
- **Verification:** Unit tests in `Batch65CoreSettingsAndPlayerTest.kt`.

### Pass 1111: `StorageCleanupGate` Status Descriptors & Lifecycle (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/storage/StorageCleanupGate.kt`
- **Changes:**
  - Added `val hasJob: Boolean get() = job != null`.
  - Added `fun getStatusDescription(): String` returning textual lifecycle states ("Active", "Pending", "Completed", "Idle").
  - Added `fun cancel()` for cancelling and clearing active storage cleaning job.
- **Verification:** Unit tests in `Batch65CoreSettingsAndPlayerTest.kt`.

### Pass 1112: Verification & Quality Assurance (`:core`)
- Comprehensive unit test suite implemented in `Batch65CoreSettingsAndPlayerTest.kt`.
- Detekt 0 issues across all modules (`:app`, `:core`, `:feature-l`, `:feature-r`, `:feature-x`).
- 100% unit tests passing in `:core`.
- Preserved strict zero modifications to Compose UI and P2P modules.

---

## 3. Verification & Quality Gates

| Gate | Target | Result |
|------|--------|--------|
| **Detekt Static Analysis** | 0 warnings / issues | Passed (BUILD SUCCESSFUL, 0 issues) |
| **Unit Test Suite** | 100% passing tests | Passed (27 actionable tasks, 0 failures) |
| **Compose UI Integrity** | Untouched | Fully Preserved |
| **P2P Module Integrity** | Untouched | Fully Preserved |
