# Code Review & Optimization Report — v235 (Passes 737–744)

**Date:** 2026-09-25  
**Scope:** `:core`, `:feature-x`, `:feature-l`, `:feature-r` (Non-UI: Webserver, IO/Path, Repositories, Parsers, GraphQL, API Clients)  
**Quality Gates:** `detekt` (0 issues) & `testDebugUnitTest` (100% pass)  
**Strict Constraint:** Zero changes to Compose UI; zero changes to P2P.

---

## Summary of Passes

### Pass 737 (`:core` — `LocalMediaResolver.kt`)
- Replaced `listOf(File(AppPath.l_likes), File(AppPath.l_albums))` with `arrayOf(...)`.
- Extracted helper `resolveMediaInBaseDir` and replaced `colRoot.listFiles()?.filter { }?.firstNotNullOfOrNull { }` with a single loop and early return, drastically reducing cyclomatic complexity and avoiding intermediate list / lambda allocations.
- Replaced `.filter { it.isDirectory }?.firstNotNullOfOrNull { }` in `resolveRVideoFile` and `resolveRPosterFile` with direct indexed loops.
- Replaced `colDir.listFiles()?.filter { it.isFile && it.extension == "collection" }.orEmpty()` in `resolveRCollectionCover` with a direct loop.
- Replaced string charset `"UTF-8"` with `Charsets.UTF_8.name()` in `encodePathSegment`.

### Pass 738 (`:core` — `SafePath.kt`)
- In `normalizeRelativePath`, eliminated intermediate `name.split('/').filter { it.isNotBlank() }` lists and `joinToString("/")` allocation by using a single-pass `StringBuilder` scanning with `indexOf('/')`.
- Extracted helper `isDotSegment` and used `require(!isDotSegment(...))` to prevent intermediate list allocations and ensure path traversal protection while satisfying Detekt complexity rules.

### Pass 739 (`:feature-x` — `XSite.kt`)
- In `parseDurationToMs`, replaced colon-splitting (`text.split(":").mapNotNull { }`) with helper `parseColonDuration` using index-based substring parsing, avoiding list and string array allocations.
- Replaced `text.filter { it.isDigit() }` string allocation with an in-place digit loop and overflow guard.

### Pass 740 (`:feature-x` — `SavedX_History.kt`)
- In `delete(item: ItemsX)`, replaced full-scan `list.removeAll { it.item.id == item.id }` with `indexOfFirst` and `removeAt`, stopping immediately upon match.
- In `deleteBatchByIds`, eliminated the intermediate `validIds = ids.filter { it > 0L }` list allocation by filtering inline in `ids.forEach`.

### Pass 741 (`:feature-l` — `getAlbumInfo.kt`)
- Hoisted static 45-line GraphQL query to top-level constant `GET_ALBUM_INFO_QUERY` to eliminate multiline string allocation and `trimIndent()` overhead on every album fetch.

### Pass 742 (`:feature-l` — `getLandingPageAlbumSearch.kt` & `getLandingPageAlbumTag.kt`)
- Hoisted static GraphQL queries to top-level constants `LANDING_PAGE_SEARCH_QUERY` and `LANDING_PAGE_TAG_QUERY` to avoid repeated multiline string allocation and `trimIndent()` parsing during landing page search and tag browsing.

### Pass 743 (`:feature-r` — `RedApi_Tags.kt`)
- Hoisted static `ROUTE_TAGS = Route("GET", "/v1/tags")` so that `getTags()` reuses a singleton route without per-call object allocations.

### Pass 744 (`:feature-r` — `RedApi_Search.kt` & `:feature-x` — `parserScreenTags.kt`)
- In `RedApi_Search.kt`, hoisted `EMPTY_CREATORS_RESPONSE = SearchCreatorsResponse()` for empty search text in `searchCreatorsShort`.
- Hoisted `SEARCH_GIFS_PATH` and `SEARCH_GIFS_VERIFIED_PATH` constants and deduplicated `Route` construction in `searchGifs`.
- In `parserScreenTags.kt`, decomposed parser logic by extracting `parseLastPage` and `parseTagItemVideo`, lowering cyclomatic complexity and optimizing element processing.

---

## Verification

- `./gradlew detekt` — **PASSED** (0 issues)
- `./gradlew testDebugUnitTest` — **PASSED** (100% tests passing across `:core`, `:feature-l`, `:feature-r`, `:feature-x`, `:app`)
