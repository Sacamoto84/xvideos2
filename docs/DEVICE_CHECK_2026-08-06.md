# Проверка на устройстве — правки 06.08.2026

Срез `c113373..6207f48`, 26 коммитов. Компилятор, 149 юнит-тестов, detekt и lint
зелёные — но ни одна правка на устройстве не проверялась.

Список отсортирован по цене ошибки: сверху то, где поломка стоит данных или
доступа к приложению, снизу — косметика. У каждого пункта записано, **что
сделать**, **что должно быть** и **как выглядит поломка** — последнее важнее
всего: половина сегодняшних дефектов была из тех, что не падают, а тихо делают
не то.

Отметки: ⛔ поломка блокирует работу · ⚠️ ломается тихо · ▫️ заметно сразу.

---

## Запуск 1 — падение на старте, исправлено (`5237904`)

Приложение не доживало до первого экрана:

```
java.lang.ExceptionInInitializerError
  at com.client.xvideos.r.network.http.Route.fillPlaceholders(Route.kt:35)
  at com.client.xvideos.r.network.api.RedApi_Explorer.getExplorerNiches
  at com.client.xvideos.r.common.saved.R_Saved_NichesCaches$refresh$1
Caused by: java.util.regex.PatternSyntaxException:
  Syntax error in regexp pattern near index 8
  \{(\w+)}
```

Незакрытая фигурная скобка в шаблоне подстановки из `41f2fec`. **Десктопная JVM
такое выражение принимает, движок ICU на Android — нет.** Выражение лежит в
инициализаторе файла, поэтому падал не отдельный запрос, а весь класс `RouteKt`,
и следом всё, что строит адрес: обновление кеша ниш и списка тегов на старте.
Оба идут из `SplashActivity`.

Что из этого следует для остального списка:

1. **До `5237904` не проверено ничего** — ни один пункт ниже, приложение не
   запускалось. Проходить список надо целиком, с версии `5237904` и новее.
2. **Зелёные юнит-тесты не значат, что код работает на устройстве.** Девять
   проверок `RouteTest` были зелёными: они идут на десктопной JVM с другим
   движком регулярных выражений. Это ровно та разница, ради которой список ниже
   и написан.
3. Остальные регулярные выражения проекта проверены: фигурные скобки есть ещё в
   трёх, но везде как квантификаторы (`{1,120}`, `{2}`) — валидны в обоих
   движках.

---

## A. Доступ и данные — проверять первым

### A1 ⛔ Замок приложения: ввод кода

Правка: проверка кода ушла с главного потока на `Dispatchers.Default`
(`964592f`), `onUnlock` стал suspend-лямбдой, добавлена защита от повторного
нажатия.

1. Включить код доступа в настройках, свернуть и открыть приложение заново.
2. Ввести **неверный** код 5 раз подряд.
3. Ввести верный код.

Должно быть: после ввода экран разблокируется без заметной задержки и **без
подвисания интерфейса**; после четырёх ошибок появляется «Слишком много попыток.
Подождите N с» и обратный отсчёт идёт; повторные нажатия во время проверки
ничего не ломают.

Поломка: интерфейс замирает на полсекунды при каждом вводе (значит, правка не
доехала); счётчик попыток не растёт или задержка не появляется (значит,
`resetFailedAttempts`/`registerFailedAttempt` теперь вызываются не там); двойное
нажатие запускает две проверки.

### A2 ⛔ Смена и отключение кода доступа

Правка та же (`964592f`) — диалог в настройках тоже стал асинхронным.

1. Настройки → сменить код: ввести текущий, новый, подтверждение.
2. Отключить код, введя текущий.
3. Проверить, что после отключения замок при перезапуске не появляется.

Поломка: диалог закрывается, но код не сменился; «Текущий код доступа не
подходит» на верном коде; кнопка срабатывает дважды.

### A3 ⛔ Бэкап и восстановление, полный цикл

Правка: из `XlrBackupManager` вынесены модели в `XlrBackupModels.kt`
(`6207f48`). Сам объект не трогали, но файл затронут.

1. Создать бэкап с данными всех трёх разделов.
2. Проверить размер файла — он должен быть сопоставим с прежними бэкапами.
3. Восстановить его на тех же данных.
4. Открыть L-коллекции, R-лайки и X-избранное.

Поломка: бэкап создаётся пустым или заметно меньше обычного; восстановление
падает; после восстановления пропали разделы. **Это единственное место, где
ошибка стоит пользовательских данных — делать на копии.**

### A4 ⚠️ «Поделиться» из всех трёх мест

Правка: область FileProvider сужена с «весь filesDir и весь кеш» до `store/` и
`cacheDir/L/Share` (`16d67a5`).

1. L: открыть картинку → «Поделиться» → отправить в любое приложение.
2. R: сохранённую гифку → «Поделиться».
3. Настройки → Диагностика → поделиться журналом ошибок.

Должно быть: во всех трёх случаях открывается системное меню и файл доходит.

Поломка: `IllegalArgumentException: Failed to find configured root` — значит,
какой-то путь не попал в новый `file_paths.xml`. Ошибка **мгновенная и явная**,
не тихая.

---

## B. Раздел R — сюда пришлось больше всего правок

### B1 ⛔ Обрыв сети на лентах

Правка: `cacheMediaResponse` возвращает `Result`, отказ больше не превращается в
пустую ленту (`9d84a3e`).

1. Выключить сеть.
2. Открыть R → вкладку гифок, переключить сортировку на **Week**, затем
   **Month**, **Trending**, **Top**, **Latest**.
3. То же в ленте любой ниши.

Должно быть: **на каждой** сортировке — сообщение об ошибке с возможностью
повторить.

Поломка: пустой экран без объяснения хотя бы на одной сортировке — значит,
ветка осталась на старом пути. До правки так вели себя все, кроме Latest.

### B2 ▫️ Сортировки ленты гифок

Правки: `TOP_ALLTIME` сведён к `Order.TOP` с настоящим методом (`9c0c48e`),
удалены `RECENT`/`BEST` (`78dd600`), выбор больше не сбрасывается при вводе
(`33a5628`).

1. В ленте гифок открыть меню сортировки: должно быть **Week, Month, Top,
   Trending, Latest** — пункта «All time» больше нет, вместо него «Top».
2. Выбрать **Top** — лента должна перезагрузиться и отличаться от Week.
3. Выбрать **Top**, затем ввести слово в поиск и подтвердить.
4. Очистить строку поиска.

Должно быть: в поиске набор — **Relevant, Top, Week, Month, Trending, Latest**;
выбранный «Top» при переходе к поиску **остаётся Top**; при очистке строки
остаётся Top. «Relevant» при очистке становится «Top».

Поломка: сортировка молча становится «Latest» при вводе текста — правка не
доехала. «Top» показывает те же элементы, что «Week» — значит, `getTopAllTime`
не вызывается.

### B3 ⚠️ Обновление лент по сроку кеша

Правка: у кеша ответов появился срок 6 часов, уборка на старте (`7bf15d0`).

1. Открыть ленту Week, запомнить первые элементы.
2. Закрыть и открыть приложение — лента должна открыться **мгновенно и с теми же
   элементами** (кеш работает).
3. Вернуться к ней **через 6+ часов** — состав должен обновиться.

Поломка: лента перезапрашивается каждый раз (срок не читается) или не
обновляется и через сутки (уборка не вызывается).

### B4 ▫️ Поиск гифок и подсказки

Правки: `debounce(300)` + `mapLatest` вместо запроса на каждую клавишу
(`360e292`), кодирование адреса отдано ktor (`41f2fec`).

1. Медленно набрать `cat` — подсказки появляются **после паузы**, не дёргаются.
2. Быстро набрать `corgi` и сразу стереть — список подсказок **исчезает сразу**,
   без задержки.
3. Найти `big cat` (с пробелом).
4. Найти `кот` (кириллица).
5. Найти `50%` — раньше `%` не экранировался.
6. Подтвердить поиск заведомо бессмысленного `zzzqqq`.

Должно быть: 3–5 дают выдачу; `zzzqqq` даёт **пустой результат**, а не полную
ленту.

Поломка: `zzzqqq` вернул ленту — параметр снова не доходит; кириллица или `%`
дают пустоту или ошибку — кодирование сломалось; подсказки отстают от ввода на
несколько слов — `mapLatest` не отменяет предыдущий запрос.

### B5 ▫️ Ниши: заглушки списка

Правка: `Refresh` и `RefreshMini` вынесены в `NichesRefresh.kt` (`6207f48`),
сортировка ниш и раньше была клиентской (`c6d385a`).

1. Очистить кеш ниш (или на свежей установке) → вкладка ниш.
2. Нажать «Скачать список» — должен идти волновой прогресс, по окончании список
   появляется.
3. Когда список устареет — вверху строка «Старый список Niches, возраст N часов»
   с круглой кнопкой обновления.
4. Перебрать все шесть сортировок: Subscribers ↓↑, Post ↓↑, Name A-Z, Z-A.

Поломка: заглушка не появляется или прогресс не движется; сортировка по имени
ничего не делает (она **клиентская**, серверной по имени у API нет).

### B6 ⚠️ «Refresh» в ленте ниши

Правка: в `ItemNailsPagingSource` добавлен пропущенный `return` — раньше
«Refresh» уходил в сеть с пустым `order` (`9d84a3e`).

1. Открыть ленту ниши, в меню сортировки выбрать **Refresh**.

Должно быть: лента перезагружается штатно.

Поломка: пустая лента или ошибка — значит, ветка всё ещё уходит в сеть.

### B7 ▫️ Профиль автора

Правка: из `Order` удалены `RECENT` и `BEST` (`78dd600`).

1. Открыть профиль активного автора (постил недавно).
2. Перебрать сортировки: Top, Latest, Oldest, Top28, Trending.
3. Открыть профиль автора, не постившего давно, выбрать **Top28**.

Должно быть: в меню **нет** «Recent» и «Best»; у неактивного автора Top28 и
Trending дают **пусто — это норма**, а не поломка (окно по времени, проверено на
API).

### B8 ▫️ Полный экран R

Правки: оверлей вынесен в `RedFullScreenOverlay.kt` (`6207f48`),
`collectAsState` → `collectAsStateWithLifecycle` (`c17b907`).

1. Открыть гифку на весь экран из ленты.
2. Проверить оверлей: аватар автора, значок коллекции, лайк, подписка,
   загрузка, кнопка профиля.
3. Пролистать вертикально между гифками.
4. Свернуть приложение на минуту, вернуться.

Поломка: оверлей пуст или без аватара; после сворачивания список загрузок не
обновился.

### B9 ▫️ Список загрузок и блокировок

Правка: `collectAsStateWithLifecycle` в четырёх экранах (`c17b907`).

1. Начать загрузку гифки, свернуть приложение, вернуться — прогресс должен быть
   актуальным.
2. Открыть экран блокировок, добавить и убрать блокировку.
3. История поиска: ввести запрос, подтвердить, открыть поле снова — запрос в
   истории.

---

## C. Раздел X

### C1 ⚠️ Превью в лентах

Правка: признак отказа `"null"` заменён на настоящий `null` в трёх парсерах
(`a35c689`).

1. Открыть ленту X, пролистать несколько экранов.
2. Тапнуть по карточкам — должно включаться видео-превью.
3. Открыть вкладку тегов, там же.
4. Открыть **избранное** — записи там сделаны прошлыми версиями и могут
   содержать старую строку `"null"`.

Должно быть: превью играют; там, где превью нет, остаётся картинка.

Поломка: чёрные карточки вместо превью; падение при открытии избранного.

### C2 ⛔ Плеер X

Правки: скрипт плеера ищется по содержимому, а не по шестому месту;
`parseHTML5Player` возвращает `null`, если нет ни одного источника (`a35c689`).

1. Открыть любое видео из ленты — должно играть.
2. Открыть видео на весь экран.
3. Открыть видео **со страницы тегов** и из **избранного**.
4. Скачать видео (тот же разбор используется загрузчиком).

Поломка: «нет источника» или вечная загрузка — значит, скрипт не находится.
Это самая вероятная поломка дня: разбор HTML переписан, а страницы сайта я не
видел.

### C3 ⚠️ WebView раздела X

Правка: `allowContentAccess = false`, явный `MIXED_CONTENT_NEVER_ALLOW`
(`16d67a5`).

1. Открыть экран, который тянет страницу через WebView (теги, страница видео
   при обычном отказе).

Поломка: страница не догружается, пустой результат разбора. Проверять на слабой
сети тоже — там таймаут 45 с.

---

## D. Раздел L

### D1 ⚠️ Полноэкранный просмотр L — самый крупный передел

Правка: файл на 800 строк разделён на пять (`e72ac21`). Перенос дословный, но
именно здесь проход 4 находил наложение страниц и конфликт жестов.

1. Открыть картинку из альбома на весь экран.
2. **Свайп** влево-вправо между страницами — плавно, без наложения предыдущей
   картинки поверх текущей.
3. **Зум** щипком и двойным тапом; после зума пролистать — зум должен
   сбрасываться, увеличенная картинка не должна вылезать на соседнюю страницу.
4. **Поворот** кнопкой.
5. Открыть **видео** в альбоме: постер до старта, звук, перемотка, кнопка mute.
6. Пролистать на соседнюю страницу и обратно — видео не должно продолжать играть
   в фоне.
7. Кнопка **«Информация»**: диалог со всеми полями, ссылки кликабельны и
   открываются в браузере.
8. Проверить **прозрачную** картинку — под ней шахматная подложка.

Поломка: рывки при свайпе, предыдущая картинка поверх текущей, зум не
сбрасывается, диалог пуст или ссылки не кликаются.

### D2 ▫️ Альбом L

Правки: состояние антибот-защиты переведено на `StateFlow` (`3dd0c55`), панель
ошибок и фильтры вынесены в отдельные файлы (`6207f48`).

1. Открыть альбом, дождаться загрузки страниц.
2. Довести до срабатывания антибот-защиты (быстро открыть несколько альбомов
   подряд) — должна появиться панель с обратным отсчётом и кнопкой повтора.
3. Нажать повтор — страницы должны дозагрузиться.
4. Фильтры по жанру и по аудитории.

Поломка: панель не появляется вовсе или появляется и не обновляет отсчёт —
значит, `StateFlow` не подписан.

### D3 ▫️ Список альбомов L

Правка: стартовое число страниц пейджера 10 → 1 (`3dd0c55`), удалён мёртвый
`Saver`.

1. Открыть список альбомов.
2. Сразу, не дожидаясь загрузки, попытаться листать вправо.

Должно быть: до ответа сети листать некуда; после загрузки — реальное число
страниц.

Поломка: листается в пустоту (значит, заглушка вернулась) или, наоборот,
листание не работает и после загрузки.

### D4 ▫️ Коллекции L

Правка: верхняя панель, диалог умных коллекций и состояние вынесены в отдельные
файлы (`6207f48`).

1. Вкладка коллекций: верхняя панель, выбор коллекции.
2. Сортировка коллекций (по дате, по имени).
3. Диалог «умные коллекции» — предложения, создание.

---

## E. P2P

### E1 ⚠️ Отправка: уход посреди подготовки

Правка: скоуп экрана отправки теперь отменяется (`529942a`).

1. Начать отправку **большого альбома L** — упаковка бандла идёт с прогрессом.
2. **Уйти с экрана назад в середине упаковки.**
3. Подождать минуту, вернуться на экран отправки.

Должно быть: упаковка прекращается сразу; при возврате начинается заново.

Поломка: при возврате прогресс продолжается с середины или файл появляется сам
— значит, старая корутина жива.

### E2 ▫️ Приём

Правка: убран `!!` и условный сбор состояния (`529942a`), `collectAsState` →
`collectAsStateWithLifecycle`.

1. Открыть экран приёма — состояние «ожидание».
2. Принять файл с другого устройства целиком.
3. Свернуть приложение во время приёма и вернуться.

Поломка: экран приёма падает при открытии; состояние не обновляется.

---

## F. Сборка и общее

### F1 ⛔ Релизная сборка ставится и запускается

Правки затронули сборку: convention-плагин (`f14739d`, `5b52453`), сужение
зависимостей до `implementation` (`239ed39`), `lint checkDependencies`
(`b751bef`).

1. Собрать **release** APK и поставить на устройство.
2. Запустить, пройти по всем трём разделам.

Поломка: приложение не ставится или падает на старте — значит, R8 вырезал
лишнее либо какая-то зависимость ушла из runtime. **Важно:** `implementation`
оставляет библиотеку в runtime, так что падать не должно; но проверить надо
именно release, а не debug.

### F2 ⚠️ Темы и оформление

Правка: `material` и `core-splashscreen` **намеренно** оставлены в `api` из-за
XML-тем (`239ed39`).

1. Экран заставки при старте.
2. Диалоги, кнопки, переключатели в настройках.

Поломка: сломанные стили, чёрный текст на чёрном, отсутствующая заставка —
значит, зависимость с ресурсами всё-таки ушла из compile-класспата.

### F3 ▫️ Вариант benchmark

Правка: вариант вынесен в convention-плагин для библиотек (`5b52453`,
`f14739d`).

1. Собрать вариант `benchmark`.

Должно быть: собирается; у `:app` он по-прежнему с R8 и подписью, как release.

---

## Что проверять НЕ надо

Эти изменения проверены иначе и глазами ничего не добавят:

- **удаление семи мёртвых методов** (`fdeb121`) — вызовов не было, компилятор
  подтвердил;
- **значения `Order`** (`3b38260`, `e4f0789`) — проверены запросами к живому API,
  таблица в `docs/redgifs-api.md`;
- **параметр `order` у `getExplorerNiches`** (`c6d385a`) — сверены выдачи первых
  двух страниц до и после;
- **`Route`** (`41f2fec`) — девять юнит-тестов плюс проверка на живом API;
  на устройстве достаточно пункта B4;
- **разбор X** (`a35c689`) — двенадцать юнит-тестов на фикстурах, но **фикстуры
  писал я, а не снимал с сайта**, поэтому C1 и C2 всё равно обязательны.
