# Code Review & Architecture Refactoring Report — Batch 72 (v288)
**Date:** 2026-09-26  
**Scope:** Passes 1161–1168 (`:core`)

---

## 1. Executive Summary

Batch 72 (v288) conducts an architectural review of `:core` non-UI components: application lock timeouts, throttle backoff calculations, column count selection, scroll button visual effect definitions, thumbnail size constants, backup archive models, and centralized filesystem path utilities. It adds name-based lookups and collection registries (`fromNameOrNull`, `fromNameOrDefault`, `allNames`) to `AppLockTimeout`, free attempt checks, state resets, lockout duration math, and remaining seconds calculations (`isFreeAttemptAvailable`, `reset`, `remainingSeconds`, `calculateLockoutDurationMs`) to `AppLockThrottle`, enabled status checks and column count clamping (`hasEnabledColumns`, `isColumnEnabled`, `validateColumnCount`) to `ColumnSelect`, subtitle predicates and name registries (`hasSubtitle`, `fromNameOrNull`, `isValidName`, `allNames`) to `ScrollButtonEffect`, value collections and name lookups (`allValues`, `allNames`, `fromNameOrNull`, `fromNameOrDefault`) to `ThumbnailsSize`, backup report addition and section predicates (`add`, `hasFiles`, `hasBytes`, `isRootSection`, `withLMode`, `withRMode`) to `XlrBackupModels`, and file existence and directory resolvers (`exists`, `isDirectory`, `resolveChildFile`) to `AppPath`.

---

## 2. Detailed Changes by Pass

### Pass 1161: `AppLockTimeout` Name Lookups & Registries (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/applock/AppLockTimeout.kt`
- **Changes:**
  - Added `fun fromNameOrNull(name: String?): AppLockTimeout?`.
  - Added `fun fromNameOrDefault(name: String?, default: AppLockTimeout): AppLockTimeout`.
  - Added `val allNames: List<String> = entries.map { it.name }`.
- **Verification:** Unit tests in `Batch72CoreSettingsAndAppLockTest.kt`.

### Pass 1162: `AppLockThrottle` State Resets & Backoff Mathematics (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/applock/AppLockThrottle.kt`
- **Changes:**
  - In `State`: added `val isFreeAttemptAvailable: Boolean` and `fun reset(): State`.
  - Added `fun calculateLockoutDurationMs(attempts: Int): Long`.
  - Added `fun remainingSeconds(state: State, wallNow: Long, elapsedNow: Long): Long`.
- **Verification:** Unit tests in `Batch72CoreSettingsAndAppLockTest.kt`.

### Pass 1163: `ColumnSelect` Bounds Validation & Status Checks (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/settings/ColumnSelect.kt`
- **Changes:**
  - Added `fun hasEnabledColumns(flags: List<Boolean>, minCol: Int, maxCol: Int): Boolean`.
  - Added `fun isColumnEnabled(flags: List<Boolean>, column: Int): Boolean`.
  - Added `fun validateColumnCount(columnCount: Int, minCol: Int, maxCol: Int): Int`.
- **Verification:** Unit tests in `Batch72CoreSettingsAndAppLockTest.kt`.

### Pass 1164: `ScrollButtonEffect` Subtitles & Name Parsers (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/settings/ScrollButtonEffect.kt`
- **Changes:**
  - Added `val hasSubtitle: Boolean get() = subtitle.isNotBlank()`.
  - Added `fun fromNameOrNull(name: String?): ScrollButtonEffect?`.
  - Added `fun isValidName(name: String?): Boolean`.
  - Added `val allNames: List<String> = entries.map { it.name }`.
- **Verification:** Unit tests in `Batch72CoreSettingsAndAppLockTest.kt`.

### Pass 1165: `ThumbnailsSize` Value Lists & Name Lookups (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/settings/ThumbnailsSize.kt`
- **Changes:**
  - Added `val allValues: List<String> = entries.map { it.value }`.
  - Added `val allNames: List<String> = entries.map { it.name }`.
  - Added `fun fromNameOrNull(name: String?): ThumbnailsSize?`.
  - Added `fun fromNameOrDefault(name: String?, default: ThumbnailsSize): ThumbnailsSize`.
- **Verification:** Unit tests in `Batch72CoreSettingsAndAppLockTest.kt`.

### Pass 1166: `XlrBackupModels` Report Arithmetic & Mode Mutators (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/backup/XlrBackupModels.kt`
- **Changes:**
  - In `XlrBackupReport`: added `val hasFiles: Boolean`, `val hasBytes: Boolean`, and `fun add(other: XlrBackupReport): XlrBackupReport`.
  - In `XlrBackupItem`: added `val isRootSection: Boolean` and `val hasFiles: Boolean`.
  - In `XlrBackupOptions`: added `fun withLMode(mode: XlrBackupContentMode)` and `fun withRMode(mode: XlrBackupContentMode)`.
- **Verification:** Unit tests in `Batch72CoreSettingsAndAppLockTest.kt`.

### Pass 1167: `AppPath` Path Inspection & Child Resolvers (`:core`)
- **Location:** `core/src/main/java/com/client/xvideos/common/AppPath.kt`
- **Changes:**
  - Added `fun exists(path: String?): Boolean`.
  - Added `fun isDirectory(path: String?): Boolean`.
  - Added `fun resolveChildFile(parentPath: String, childName: String): File`.
- **Verification:** Unit tests in `Batch72CoreSettingsAndAppLockTest.kt`.

### Pass 1168: Verification & Quality Assurance (`:core`)
- Comprehensive unit test suite implemented in `Batch72CoreSettingsAndAppLockTest.kt`.
- Detekt static analysis: 0 warnings / issues across all modules (`:app`, `:core`, `:feature-l`, `:feature-r`, `:feature-x`).
- 100% unit tests passing in `:core`.
- Strict zero modifications to Compose UI and P2P modules.

---

## 3. Verification & Quality Gates

| Gate | Target | Result |
|------|--------|--------|
| **Detekt Static Analysis** | 0 warnings / issues | Passed (BUILD SUCCESSFUL, 0 issues) |
| **Unit Test Suite** | 100% passing tests | Passed (27 actionable tasks, 0 failures) |
| **Compose UI Integrity** | Untouched | Fully Preserved |
| **P2P Module Integrity** | Untouched | Fully Preserved |
