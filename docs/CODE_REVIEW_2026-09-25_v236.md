# Code Review & Optimization Report — v236 (Passes 745–752)

**Date:** 2026-09-25  
**Scope:** `:core`, `:feature-x`, `:feature-l`, `:feature-r` (Non-UI: Webserver, String/Formatting Utils, Parsers, GraphQL, HTTP API Clients)  
**Quality Gates:** `detekt` (0 issues) & `testDebugUnitTest` (100% pass)  
**Strict Constraint:** Zero changes to Compose UI; zero changes to P2P.

---

## Summary of Passes

### Pass 745 (`:core` — `LocalLibraryProvider.kt`)
- Hoisted `DEFAULT_SECTIONS = listOf("ALL", "X", "R", "L", "COLLECTIONS")` in `WebLibraryResponse` to eliminate repeated default list allocations.
- In `loadXItems()`, replaced the 2-pass `.filter { mp4 }.associateBy { }` and `.filter { info }` scans with a single pass over `files`, building `mp4Files` and `infoFiles` simultaneously with zero intermediate lists.
- Decomposed `loadLItems()` into `loadFoldersInDir` and `loadLCollectionsItems`, replacing `.filter { it.isDirectory }` chains with direct directory iteration, eliminating 3 intermediate list allocations and reducing cyclomatic complexity.
- In `parseLFolder()`, replaced `folder.listFiles()?.filter { it.isFile }?.sumOf { it.length() }` with an in-place accumulation loop.
- In `loadRFromLikes()`, replaced `.filter { it.isFile && it.extension == "likes" }.orEmpty()` with a direct array loop.

### Pass 746 (`:core` — `capitalizeEachWord.kt`)
- Rewrote `String.capitalizeEachWord()` from `lowercase().split(" ").joinToString(" ") { replaceFirstChar { ... } }` to a single-pass `StringBuilder` character loop, eliminating `split(" ")`, word string allocations, and intermediate lists.

### Pass 747 (`:core` — `toTwoDecimalPlacesWithColon.kt`)
- Replaced `String.format(Locale.US, "%.2f", this).replace('.', ':')` with direct hundredths integer arithmetic (`round(this * 100f).toLong()`), eliminating Java `Formatter` parsing and secondary `replace` string allocation on every player frame update.

### Pass 748 (`:feature-x` — `parserItemVideoTags.kt`)
- Hoisted singleton `EMPTY_TAGS_MODEL = TagsModel()` for blank HTML responses.
- Eliminated intermediate collection chains `.mapNotNull { }.toSortedSet().toList()` by inserting trimmed keywords directly into a single `TreeSet<String>` and converting once via `toList()`.
- Pre-sized `listMain` and `listPornstar` `ArrayList`s and cached `val nameSpan = selectFirst("span.name")` in `parseUploaderOrModel()` to avoid duplicate CSS selector queries.

### Pass 749 (`:feature-x` — `parserVideoPreviewFromImageUrl.kt`)
- In `parserNewCdnPreviewUrl`, replaced `parts.drop(hostIndex + 1).size < 3` with `parts.size - (hostIndex + 1) < 3`, eliminating sublist allocation.
- In `parserVideoPreviewFromImageUrl`, replaced `parts.take(videosIndex + 1)`, `folders` sublists, `buildList`, and `joinToString("/")` with a single pre-sized `StringBuilder`, eliminating 4 list allocations per video card.

### Pass 750 (`:feature-l` — `getAlbumListGraphQL.kt`)
- Hoisted the shared 47-line static GraphQL query to top-level constant `ALBUM_LIST_QUERY` (with `trimIndent()`, newline, and quote escaping precomputed once), reusing it across `getAlbumListGraphQL1` and `getAlbumListGraphQL`.
- Replaced non-short-circuiting bitwise `or` with logical `||` for `tagPlus`/`tagMinus` and `genresPlus`/`genresMinus`.
- Replaced `it.replace(" ", "_")` string literal replacement with `it.replace(' ', '_')` character replacement and pre-sized `StringBuilder(1024)`.

### Pass 751 (`:feature-l` — `getAlbumListWithAggregations.kt` & `getPicturesJson.kt`)
- In `getAlbumListWithAggregations.kt`, hoisted static 37-line GraphQL query to `ALBUM_LIST_WITH_AGGREGATIONS_QUERY`, replaced bitwise `or` with `||`, and used `replace(' ', '_')`.
- In `getPicturesJson.kt`, hoisted static 40-line GraphQL query to top-level constant `LIST_ALBUM_PICTURES_QUERY` to eliminate per-page string allocation and `trimIndent()` overhead.

### Pass 752 (`:feature-r` — `RedApi.kt` & `RedApi_Explorer.kt`)
- In `RedApi_Explorer.kt`, hoisted `PATH_EXPLORER_NICHES` constant.
- In `RedApi.kt`, hoisted static route path templates `PATH_TOP7`, `PATH_TOP28`, `PATH_TOP`, `PATH_TRENDING`, `PATH_LATEST`, `PATH_USER_SEARCH_TAGS`, `PATH_USER_SEARCH`, `PATH_USER_SEARCH_TYPE_TAGS`, and `PATH_USER_SEARCH_TYPE`.

---

## Verification

- `./gradlew detekt` — **PASSED** (0 issues)
- `./gradlew testDebugUnitTest` — **PASSED** (100% tests passing across `:core`, `:feature-l`, `:feature-r`, `:feature-x`, `:app`)
